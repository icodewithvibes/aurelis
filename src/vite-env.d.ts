/// <reference types="vite/client" />

/**
 * Injected by `define` in vite.config.ts — the commit and time this
 * bundle was built. Settings shows it so a stale installed PWA can be
 * ruled out by reading one line off the phone.
 */
declare const __BUILD_STAMP__: string;
