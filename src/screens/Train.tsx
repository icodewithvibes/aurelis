import { PlaceholderScreen } from "../components/PlaceholderScreen";

export function Train() {
  return (
    <PlaceholderScreen
      id="train-heading"
      title="Train"
      intent="Your split, your session, the work in front of you."
      stageNote="The workout logger, ASF import, sets, reps, RPE, and rest timers arrive in Stage 2. This surface is a placeholder."
    />
  );
}
