# Retired — the brief lives in the vault now

This file was the resume-here brief until 2026-08-16. It is no longer
maintained and everything it said about state is out of date: it still
described the app as AURELIS, `main` at PR #29, 647 tests, and 51
exercise images waiting to be regenerated. All of that has since
changed, which is exactly why it had to stop being a second source of
truth.

**The current brief is `02 - FORGE/FORGE Next Session Brief.md` in the
Obsidian vault** at `C:\Users\Yuriel Gonzalez\Documents\Brain`, which
Claude Code reads at the start of every session via `CLAUDE.md`.

Nothing was lost. The engineering lessons this file carried were moved
to `02 - FORGE/FORGE Engineering Gotchas.md` in the same vault, and the
per-topic notes it referenced were already there.

This stub is kept rather than deleted so anyone who arrives at the old
path gets redirected instead of silently reading nothing — or worse,
finding the stale version in git history and trusting it.
